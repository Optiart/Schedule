CREATE TABLE [dbo].[Results]
(
	id INT PRIMARY KEY IDENTITY(1,1),
	tab_id INT NOT NULL,
	result VARCHAR(5096) NOT NULL,
	chain VARCHAR(1024) NOT NULL

	CONSTRAINT FK_Results_tab FOREIGN KEY (tab_id) REFERENCES Tabs(id) ON DELETE CASCADE
);
GO